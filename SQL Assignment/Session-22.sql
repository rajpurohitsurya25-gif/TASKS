/*
1.
Use pandas' read_sql() function to load all records from a 'restaurants' table (with columns like name, cuisine, rating)
 in your local SQLite database into a DataFrame, then display the first 5 rows.*/
 
 /*
2.
In a Jupyter Notebook, use the %sql magic command to run a SQL query that selects all movies with a rating above 8 from a
 'movies' table, and display the results in the notebook.*/
 
/*
3.
Combine SQL and Python to analyze Zomato-style order data: use pandas read_sql() to load the last 100 orders from an 
'orders' table, then use DataFrame methods to find the top 3 most ordered food items.*/

/*
4.
After loading IPL match data from a 'matches' table into a pandas DataFrame, create a bar chart showing the number of matches
 played by each team using matplotlib or seaborn.<br><br><em><strong>Hint:</strong> Use groupby() in pandas to count matches per team before plotting.</em>*/
 
 /*
5.
Use ChatGPT or Copilot to help you write a Jupyter Notebook cell that connects to a SQLite database, runs a SQL query to fetch all
 users who have placed more than 5 orders, and loads the result into a pandas DataFrame.*/